#include <iostream>
#include <string>
#include <vector>

using namespace std;

int add(int a, int b){
	return a+b;
}
int sub(int a, int b){
	return a-b;
}
int divide(int a, int b){
	return a/b;
}
int multi(int a, int b){
	return a*b;
}

int loop(string input){
	
	for(int i=0; i<input.size(); i++){

	}
}

int main(){
	string input;
	cout<<"Input : ";
	cin>>input;

	string tmp1, tmp2; 
	vector <int> v;

	/*for(int i=0; i<input.size(); i++){
		if(input[i]=='*'){
			tmp1=input[i-1];
			tmp2=input[i+1];
			v.push_back(multi(stoi(tmp1),stoi(tmp2)));
		}
		else if(input[i]=='/'){
			tmp1=input[i-1];
			tmp2=input[i+1];
			v.push_back(divide(stoi(tmp1),stoi(tmp2)));
		}
		else if(input[i]=='('){

		}

	}*/

	for(int i=0; i<input.size(); i++){

	}
	

	//for(int i=0; i<input.size(); i++){

	//}
}